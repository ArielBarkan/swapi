export class Planet {
  private name: string;
  private url: string;
  private population: string;

  constructor(name: string, url: string, population: string) {
    this.name = name;
    this.url = url;
    this.population = population;
  }
}
